name: Update Trilium in to Napptive Playground
on:
  push:
    branches:
      - 'main'
    paths:
      - "trilium-template/napptive/componentDEV/**"
      - "trilium-template/napptive/componentPROD/**"
      - "trilium-template/napptive/componentPVC/**"
jobs:
  deploy:
    name: New version upload
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - base: 'application-test'
            tag: 'DEV'
          - base: 'latest'
            tag: 'PROD'
          - base: 'storage'
            tag: 'PVC'
    steps:
      - uses: actions/checkout@v2
      - uses: napptive-actions/catalog-push-action@v4.2.0
        env:
          PLAYGROUND_PAT: ${{ secrets.PLAYGROUND_PAT }}
        with:
          applicationPath: ./trilium-template/napptive/component${{ matrix.tag }}/
          namespace: "davma-io"
          applicationName: "Trilium"
          tag: "${{ matrix.base }}"