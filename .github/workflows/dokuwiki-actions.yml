name: Update DokuWiki in to Napptive Playground
on:
  push:
    branches:
      - 'main'
    paths:
      - "dokuwiki-template/napptive/componentDEV/**"
      - "dokuwiki-template/napptive/componentPROD/**"
      - "dokuwiki-template/napptive/componentPVC/**"
jobs:
  deploy:
    name: New version upload
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - base: 'application-test'
            tag: 'DEV'
          # - base: 'latest'
          #   tag: 'PROD'
          # - base: 'storage'
          #   tag: 'PVC'
    steps:
      - uses: actions/checkout@v2
      - uses: napptive-actions/catalog-push-action@v4.2.0
        env:
          PLAYGROUND_PAT: ${{ secrets.PLAYGROUND_PAT }}
        with:
          applicationPath: ./dokuwiki-template/napptive/component${{ matrix.tag }}/
          namespace: "davma-io"
          applicationName: "DokuWiki"
          tag: "${{ matrix.base }}"